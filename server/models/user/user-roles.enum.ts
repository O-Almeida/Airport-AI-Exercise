// UserRoles enum has all the possible types of roles for a User
/**
 * A enum with the different roles that a user can have.
 * */
export enum UserRoles {
  AGENT = "agent",
  PASSENGER = "passenger",
}
